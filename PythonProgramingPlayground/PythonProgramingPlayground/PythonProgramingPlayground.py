#You use '#' for comments, ok
#I have no internet rn so I can't look up for loops :c
#Seems as if <var>++ doesn't work since it isn't a number directly but an object, however <var> = <var> + 1 works
x = 0
while True:
	print(x)
	x = x +  1
